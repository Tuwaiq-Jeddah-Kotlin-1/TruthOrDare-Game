<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.truthordaresaudi">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".notification.NotificationHelper"
        android:allowBackup="true"
        android:icon="@drawable/logotordm"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logotordm"
        android:supportsRtl="true"
        android:theme="@style/Theme.TruthOrDareSaudi">

        <activity
            android:name="com.example.truthordaresaudi.ui.MyMainActivity"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>